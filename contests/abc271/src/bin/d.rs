// use proconio::{fastout, input};

// #[fastout]
// 深さ優先探索？
fn main() {
    //     input! {
    //         n:i32,
    //         s:i32,
    //         ab:[(i32,i32);n]
    //     }

    //     println!(
    //         "{} {}",
    //         partial_sum(s, &ab, "".to_string()).0,
    //         partial_sum(s, &ab, "".to_string()).1
    //     )
    // }
    // fn partial_sum(k: i32, a: &[(i32, i32)], fb: String) -> (bool, String) {
    //     let mut str = fb;
    //     if a.is_empty() {
    //         // 最後まで探索した場合
    //         let mut answer = (false, "".to_string());
    //         if k == 0 {
    //             answer = (true, str);
    //         }
    //         answer
    //     } else {
    //         // まだ数が残っているとき
    //         // 表か裏から解く
    //         // 表から解いたときと裏から解いた時で場合分けする
    //         partial_sum(k - a[0].0, &a[1..], 'H') || partial_sum(k - a[0].1, &a[1..], 'T')
    //     }
}
